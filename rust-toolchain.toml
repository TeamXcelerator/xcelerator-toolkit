# Development builds use the stable channel. The minimum compiler version is
# declared in Cargo.toml; qualification versions are recorded in docs/VALIDATION.md.
# For reproducible experiments, record rustc -Vv and set RUSTUP_TOOLCHAIN to
# the exact installed version used for the run.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
